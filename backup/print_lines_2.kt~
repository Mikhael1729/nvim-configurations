import java.io.File 

fun handlePrintLine(line: String): Unit {
    if(line.length <= 10)
        System.err.println(line)
    else
        println(line)
}

fun main(args: Array<String>) {
	File(args[0]).forEachLine { handlePrintLine(it) }
}

