import React from "react"
import { Typography, Button, Divider } from "@material-ui/core"
import { makeStyles, createStyles, Theme } from "@material-ui/core/styles"
import Grid from "@material-ui/core/Grid"
import Card from "@material-ui/core/Card"
import CardActions from "@material-ui/core/CardActions"
import CardContent from "@material-ui/core/CardContent"
import logo from "images/pexels-photo-220453.jpeg"
import { RouteComponentProps } from "@reach/router";
import { UserProfileAndStoreProps } from ".";
import SC from "models/screen-component";

const useStyles = makeStyles((theme: Theme) =>
  createStyles({
    root: {
      flexGrow: 1,
    },
    media: {
      width: 175,
    },
    imageCard: {
      marginTop: "40px",
      padding: theme.spacing(2),
      textAlign: "center",
      color: theme.palette.text.secondary,
    },
    button: {
      marginTop: "-45px",
      marginLeft: "40px",
    },
    userInfo: {
      marginTop: "40px",
      marginLeft: "100px",
      textAlign: "left",
    },
    userInfo2: {
      marginTop: "-450px",
      marginLeft: "350px",
      textAlign: "left",
    },
  })
)

export interface IUserProfileProps extends RouteComponentProps {
  pageContext: { breadcrumbName: string }
}

const UserProfile: SC<UserProfileAndStoreProps> = ({ pageContext, user }) => {
  const classes = useStyles()
  return (
    <>
      <Typography variant="h3">Perfil de Cliente</Typography>
      <Divider />
      <div className={classes.root}>
        <Grid container spacing={3}>
          <Grid item>
            <Card elevation={0} className={classes.imageCard}>
              <CardContent className={classes.media}>
                <img src={logo} width="175" />
              </CardContent>
            </Card>
            <CardActions className={classes.button}>
              <Button variant="contained" size="small" color={"primary"}>
                Cambiar Imagen
              </Button>
            </CardActions>
          </Grid>
          <div className={classes.userInfo}>
            <div>
              <Typography variant="body1">Nombres:</Typography>
              <Typography variant="h5">{user.names}</Typography>
            </div>
            <br />
            <div>
              <Typography variant="body1">Apellidos:</Typography>
              <Typography variant="h5">{user.surnames}</Typography>
            </div>
            <br />
            <div>
              <Typography variant="body1">Cedula:</Typography>
              <Typography variant="h5">{user.idCard}</Typography>
            </div>
            <br />
            <div>
              <Typography variant="body1">Fecha de Nacimiento:</Typography>
              <Typography variant="h5">{user.birthdate?.toDateString()}</Typography>
            </div>
            <br />
            <div>
              <Typography variant="body1">Numero de Telefono:</Typography>
              <Typography variant="h5">{user.number}</Typography>
            </div>
            <br />
            <div>
              <Typography variant="body1">Correo Electronico:</Typography>
              <Typography variant="h5">{user.email}</Typography>
            </div>
            <br />
            <div className={classes.userInfo2}>
              <div>
                <Typography variant="body1">Ciudad:</Typography>
                <Typography variant="h5">{user.city}</Typography>
              </div>
              <br />
              <div>
                <Typography variant="body1">Calle:</Typography>
                <Typography variant="h5">{user.street}</Typography>
              </div>
              <br />
              <div>
                <Typography variant="body1">Region:</Typography>
                <Typography variant="h5">{user.region}</Typography>
              </div>
              <br />
              <div>
                <Typography variant="body1">Genero:</Typography>
                <Typography variant="h5">{user.gender}</Typography>
              </div>
              <br />
              <div>
                <Typography variant="body1">Estado de Cliente:</Typography>
                <Typography variant="h5">{user.status}</Typography>
              </div>
            </div>
          </div>

        </Grid>
      </div>

    </>
  )
}

UserProfile.routeName = "/perfil";

export default UserProfile
