class Movement {
  date?: Date;
  description?: string;
  ammount?: number;
  code?: string;
  accountBalance?: number;

  constructor(movement: Movement) {
    this.date = movement.date;
    this.description = movement.description;
    this.ammount = movement.ammount;
    this.code = movement.code;
    this.accountBalance = movement.accountBalance;
  }
}

export default Movement;
