import React, { SFC } from "react";
import { Typography } from "@material-ui/core";
import Breadcrumbs from "../../layouts/main-layout/breadcrumbs/breadcrumbs"

interface IConsultationProps {
  pageContext: { breadcrumbName: string };
  location: any
}

const Consultation: SFC<IConsultationProps> = ({ pageContext, location }) => {
  console.log('pageContext: ', pageContext);
  return (
    <>

    <Breadcrumbs location={location} crumbLabel="Another" />
    <Typography variant="h2">Consultas</Typography>
    </>
  );
}

export default Consultation;
