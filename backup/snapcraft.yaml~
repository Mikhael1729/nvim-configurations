name: bite-snap # you probably want to 'snapcraft register <name>'
base: core18 # the base snap is the execution environment for this snap
version: '0.1' # just for humans, typically '1.2+git' or '1.3.2'
summary: This is a test to bite snapcraft by mk1729 # 79 char long summary
description: |
  bite-snap is a package to learn how to upload a package using snapcraft

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  bite-snap:
    source: https://github.com/Mikhael1729/bite-snap
    source-type: git
    # See 'snapcraft plugins'
    plugin: dotnet
    dotnet-runtime-version: 2.1.807
